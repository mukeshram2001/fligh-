{
  "name": "flight-delay-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@google/generative-ai": "^0.11.3"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
